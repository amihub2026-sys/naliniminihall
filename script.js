const menuBtn=document.getElementById('menuBtn');const navMenu=document.getElementById('navMenu');if(menuBtn&&navMenu){menuBtn.addEventListener('click',()=>navMenu.classList.toggle('show'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>navMenu.classList.remove('show')))}
const slides=document.querySelectorAll('.hero-slide');const dotsBox=document.getElementById('heroDots');let currentSlide=0;if(slides.length&&dotsBox){slides.forEach((_,i)=>{const btn=document.createElement('button');if(i===0)btn.classList.add('active');btn.addEventListener('click',()=>showSlide(i));dotsBox.appendChild(btn)})}function showSlide(index){slides[currentSlide].classList.remove('active');dotsBox.children[currentSlide]?.classList.remove('active');currentSlide=index;slides[currentSlide].classList.add('active');dotsBox.children[currentSlide]?.classList.add('active')}if(slides.length>1){setInterval(()=>showSlide((currentSlide+1)%slides.length),4200)}
let counted=false;function runCounters(){if(counted)return;const section=document.querySelector('.count-section');if(!section)return;const rect=section.getBoundingClientRect();if(rect.top<window.innerHeight-80){counted=true;document.querySelectorAll('.counter').forEach(el=>{const target=+el.dataset.target;let value=0;const step=Math.max(1,Math.ceil(target/80));const timer=setInterval(()=>{value+=step;if(value>=target){value=target;clearInterval(timer)}el.textContent=value},18)})}}window.addEventListener('scroll',runCounters);runCounters();
